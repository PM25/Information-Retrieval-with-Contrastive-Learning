---
# input data:
wiki_data: /tmp2/py/data/fever/wiki-pages
train_data: /tmp2/py/data/fever/train.jsonl
small_wiki: data/small_wiki.json

# device
device_id: 0 # cpu: -1, gpu: >= 0

# train
seed: 1009
epochs: 15
batch_size: 16
learning_rate: 0.0001
warmup_steps: 20
val_size: 0.1

log_step: 1

# model
model: Roberta
hidden_dim: 300
n_cls_layers: 3
dropout: 0.2
freeze_bert: true
